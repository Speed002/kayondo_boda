import './bootstrap';
// bootstrap bundle
import 'bootstrap';
// Note: alpine ships directly with livewire, so there's no need to instantiate it again. it is already done when livewire installs.
// alpine bundle
// import Alpine from 'alpinejs'
// window.Alpine = Alpine
// Alpine.start()
